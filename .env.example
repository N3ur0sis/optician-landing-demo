# ============================================
# OPTICIAN CMS - Environment Configuration
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values
# 3. NEVER commit the .env file to git
#
# For production deployment, generate secure values:
#   NEXTAUTH_SECRET: openssl rand -base64 32
#   POSTGRES_PASSWORD: openssl rand -base64 24
#   ADMIN_PASSWORD: Use a strong password (12+ chars, mixed case, numbers, symbols)
#
# ============================================

# =================================
# NODE ENVIRONMENT
# =================================
# Options: development | production
NODE_ENV=production

# Docker build target (matches NODE_ENV)
# Options: development | production
BUILD_TARGET=production

# =================================
# DATABASE (PostgreSQL)
# =================================
# Database name
POSTGRES_DB=optician_db

# Database user
POSTGRES_USER=optician

# Database password - CHANGE THIS!
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=CHANGE_ME_USE_SECURE_PASSWORD

# Database port (internal)
POSTGRES_PORT=5432

# Full database connection URL (auto-constructed in docker-compose)
# For local development outside Docker, use:
# DATABASE_URL="postgresql://optician:your_password@localhost:5432/optician_db?schema=public"

# =================================
# NEXTAUTH.JS AUTHENTICATION
# =================================
# Secret for JWT encryption - CHANGE THIS!
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_32

# Full URL of the application (with protocol)
# Examples:
#   Local: http://localhost:3000
#   Staging: https://staging.yourdomain.com
#   Production: https://www.yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# =================================
# INITIAL ADMIN USER
# =================================
# Email for the initial admin account
ADMIN_EMAIL=admin@yourdomain.com

# Password for the initial admin account - CHANGE THIS!
# Use a strong password (12+ chars, mixed case, numbers, symbols)
ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# =================================
# APPLICATION
# =================================
# Port the application runs on
PORT=3000

# =================================
# OPTIONAL - ANALYTICS & SEO
# =================================
# Google Analytics 4 Measurement ID (format: G-XXXXXXXXXX)
# NEXT_PUBLIC_GA_ID=

# Google Search Console verification code
# GOOGLE_SITE_VERIFICATION=
